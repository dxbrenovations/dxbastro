---
// Define props from Astro
const { title, subTitle, partners } = Astro.props;
import FullStar from "@components/ui/stars/FullStar.astro";

interface Partner {
  icon: any;
  name?: string;
  href?: string;
}

// Define TypeScript interface for props
interface Props {
  title: string;
  subTitle?: string;
  partners: Partner[];
}
---

<section
  class="mx-auto max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
>
  <!-- Title and description -->
  <div class="mx-auto mb-6 w-full space-y-1 text-center sm:w-1/2 lg:w-1/3">
    <h2
      class="text-2xl leading-tight font-bold text-balance text-neutral-800 sm:text-3xl dark:text-neutral-200"
    >
      {title}
    </h2>
    {
      subTitle && (
        <p class="leading-tight text-pretty text-neutral-600 dark:text-neutral-400">
          {subTitle}
        </p>
      )
    }
  </div>
  <div
    class="flex flex-col items-center justify-center gap-y-2 sm:flex-row sm:gap-x-12 sm:gap-y-0 lg:gap-x-24"
  >
    <div class="grid grid-cols-1 gap-6 lg:grid-cols-3 lg:gap-8">
      <!-- Review 1 -->
      <div
        class="flex transform cursor-pointer flex-col gap-3 rounded-md border border-neutral-300 p-5 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl dark:border-neutral-700"
      >
        <div class="flex w-full items-center gap-4">
          <div class="flex flex-col gap-0">
            <p
              class="text-lg font-semibold text-neutral-800 dark:text-neutral-200"
            >
              Ahmed Khalil
            </p>
            <p class="text-sm text-neutral-600 dark:text-neutral-400">
              The Meadows, Dubai
            </p>
          </div>
        </div>
        <p class="text-pretty text-neutral-600 dark:text-neutral-400">
          Highly recommended! My business website needed a facelift and these
          guys delivered beyond my expectations. The design is sleek and modern,
          and the user experience is seamless.
        </p>
        <div class="flex items-center gap-2">
          {
            Array(5)
              .fill(0)
              .map((_, i) => <FullStar key={i} />)
          }
          <span class="text-sm text-neutral-600 dark:text-neutral-400"
            >5.0 rating</span
          >
        </div>
      </div>

      <!-- Review 2 -->
      <div
        class="flex transform cursor-pointer flex-col gap-3 rounded-md border border-neutral-300 p-5 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl dark:border-neutral-700"
      >
        <div class="flex w-full items-center gap-4">
          <div class="flex flex-col gap-0">
            <p
              class="text-lg font-semibold text-neutral-800 dark:text-neutral-200"
            >
              Fatima Ali
            </p>
            <p class="text-sm text-neutral-600 dark:text-neutral-400">
              Business Bay, Dubai
            </p>
          </div>
        </div>
        <p class="text-pretty text-neutral-600 dark:text-neutral-400">
          The team understood exactly what I envisioned for my online store.
          They are professional, efficient, and a pleasure to work with. My
          sales have seen a significant boost thanks to their amazing design
          work!
        </p>
        <div class="flex items-center gap-2">
          {
            Array(5)
              .fill(0)
              .map((_, i) => <FullStar key={i} />)
          }
          <span class="text-sm text-neutral-600 dark:text-neutral-400"
            >5.0 rating</span
          >
        </div>
      </div>

      <!-- Review 3 -->
      <div
        class="flex transform cursor-pointer flex-col gap-3 rounded-md border border-neutral-300 p-5 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl dark:border-neutral-700"
      >
        <div class="flex w-full items-center gap-4">
          <div class="flex flex-col gap-0">
            <p
              class="text-lg font-semibold text-neutral-800 dark:text-neutral-200"
            >
              Mohammed H.
            </p>
            <p class="text-sm text-neutral-600 dark:text-neutral-400">
              Jumeirah Lake Towers, Dubai
            </p>
          </div>
        </div>
        <p class="text-pretty text-neutral-600 dark:text-neutral-400">
          Exceptional service! They are quick, creative, and their attention to
          detail is commendable. The new website has greatly improved my online
          presence. Will definitely recommend them!
        </p>
        <div class="flex items-center gap-2">
          {
            Array(5)
              .fill(0)
              .map((_, i) => <FullStar key={i} />)
          }
          <span class="text-sm text-neutral-600 dark:text-neutral-400"
            >5.0 rating</span
          >
        </div>
      </div>
    </div>

    <!-- Clients Group SVGs -->
    <!-- {
      partners.map((partner) => (
        <a href={partner.href} target="_blank" rel="noopener noreferrer">
              <div set:html={partner.icon} />
        </a>
      ))
    } -->
  </div>
</section>
